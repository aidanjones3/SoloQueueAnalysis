CREATE TABLE ACCOUNTID
(
  name      VARCHAR(20),
  region    VARCHAR(20),
  sumLvl    VARCHAR(20),
  puuid     VARCHAR(100),
  accountId VARCHAR(70),
  id        VARCHAR(70),
  epoch     DOUBLE PRECISION(20,0)
)
